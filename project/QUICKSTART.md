# N-Echo 프로젝트 빠른 시작 가이드

## 🚀 5분 안에 시작하기

### 옵션 1: Python 서버 + Python 클라이언트

#### 터미널 1 (서버)
```bash
cd /home/jeahun/n-echo-project
python3 python_server.py
```
#### 터미널 2 (클라이언트)/
```bash
cd /home/jeahun/n-echo-project
python3 python_client.py
```

#### 사용 예시
```
에코 횟수 (n): 3
에코할 메시지: Hello, World!
```

---

### 옵션 2: Java 서버 + Python 클라이언트

#### 터미널 1 (Java 서버 설정 및 실행)
```bash
cd /home/jeahun/n-echo-project

# 최초 1회 설정 (JSON 라이브러리 다운로드 및 컴파일)
./setup_java.sh

# 서버 실행
./run_java_server.sh
```

#### 터미널 2 (Python 클라이언트)
```bash
cd /home/jeahun/n-echo-project
python3 python_client.py
```

---

## 🌐 원격 서버 연결

### 서버 측 (예: Ubuntu)
```bash
# 서버 IP 확인
hostname -I

# 서버 실행 (0.0.0.0으로 모든 인터페이스에서 접속 허용)
python3 python_server.py 5000
```

### 클라이언트 측 (예: Windows)
```bash
# 서버 IP로 연결 (예: 192.168.1.100)
python3 python_client.py 192.168.1.100 5000
```

---

## 📝 테스트 시나리오

### 기본 테스트
```
n = 3
message = "Hello, TCP/IP!"
→ 메시지가 3번 에코됨
```

### 한글 테스트
```
n = 5
message = "안녕하세요!"
→ 한글 메시지가 5번 에코됨
```

### 에러 테스트
```
n = -1
message = "test"
→ 에러 메시지: "n은 양의 정수여야 합니다."
```

---

## 🔧 포트 변경

### 포트 8080 사용
```bash
# 서버
python3 python_server.py 8080

# 클라이언트
python3 python_client.py localhost 8080
```

---

## 💡 팁

1. **종료하기**: Ctrl+C (서버), 'quit' 입력 (클라이언트)
2. **멀티 클라이언트**: 여러 터미널에서 동시에 클라이언트 실행 가능
3. **방화벽**: 원격 연결 시 포트 5000을 방화벽에서 허용해야 함

---

## 📚 더 알아보기

- 자세한 사용법: `README.md`
- 과제 보고서: `REPORT.md`
- 소스 코드 분석은 각 파일의 주석 참조

