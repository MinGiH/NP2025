# TCP/IP 소켓 프로그래밍 과제 보고서

---

## 📌 과제 정보

- **과제명:** TCP/IP 소켓 프로그래밍 (10~11장 개인 과제)
- **제출자:** [이름 입력]
- **학번:** [학번 입력]
- **제출일:** 2025년 11월 3일
- **개발 환경:**
  - 서버: Ubuntu Linux 5.4.0
  - 클라이언트: Windows / Linux
  - 언어: Python 3.x
  - 프로토콜: TCP/IP

---

## 📝 과제 요구사항

TCP/IP를 사용하여 다음 3가지 서버-클라이언트 프로그램을 구현하고,
Windows와 Linux 간 크로스 플랫폼 통신을 테스트

1. **Time 서버** - 현재 시간을 클라이언트에게 전송
2. **Echo 서버** - 클라이언트 메시지를 그대로 반환
3. **Number 서버** - 숫자 맞추기 게임 제공

---

## 🏗️ 프로그램 구조

### 디렉토리 구조
```
tcp_socket_programming/
│
├── 1_time_server/
│   ├── time_server.py    # Time 서버 프로그램
│   └── time_client.py    # Time 클라이언트 프로그램
│
├── 2_echo_server/
│   ├── echo_server.py    # Echo 서버 프로그램
│   └── echo_client.py    # Echo 클라이언트 프로그램
│
├── 3_number_server/
│   ├── number_server.py  # Number 게임 서버 프로그램
│   └── number_client.py  # Number 게임 클라이언트 프로그램
│
├── README.md             # 상세 사용 설명서
├── 실행방법.txt           # 빠른 실행 가이드
├── 테스트결과.txt         # 테스트 결과 상세 보고
└── 보고서_제출용.md      # 본 보고서
```

---

## 💻 구현 내용

### 1. Time 서버 (포트 9001)

#### 서버 기능
- TCP 소켓을 생성하여 클라이언트 연결 대기
- 클라이언트 접속 시 현재 시간을 전송
- 시간 전송 후 연결 자동 종료

#### 클라이언트 기능
- 서버에 연결
- 서버로부터 현재 시간 수신
- 시간 출력 후 종료

#### 핵심 코드 설명

**서버 (time_server.py):**
```python
# TCP 소켓 생성
server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

# 주소 바인딩
server_socket.bind((host, port))

# 연결 대기
server_socket.listen(5)

# 클라이언트 연결 수락
client_socket, client_address = server_socket.accept()

# 현재 시간 전송
current_time = datetime.datetime.now()
time_str = current_time.strftime("%Y년 %m월 %d일 %H시 %M분 %S초")
message = f"서버 현재 시간: {time_str}"
client_socket.send(message.encode('utf-8'))
```

**클라이언트 (time_client.py):**
```python
# TCP 소켓 생성
client_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

# 서버 연결
client_socket.connect((host, port))

# 데이터 수신
data = client_socket.recv(1024)
message = data.decode('utf-8')
print(message)
```

---

### 2. Echo 서버 (포트 9002)

#### 서버 기능
- TCP 소켓으로 클라이언트 연결 대기
- 클라이언트로부터 메시지 수신
- 받은 메시지를 "Echo: " 접두사와 함께 반환
- 'quit' 또는 'exit' 수신 시 연결 종료

#### 클라이언트 기능
- 서버에 연결
- 사용자로부터 메시지 입력 받아 서버에 전송
- 서버 응답 출력
- 'quit' 입력 시 종료

#### 핵심 코드 설명

**서버 (echo_server.py):**
```python
while True:
    # 클라이언트로부터 데이터 수신
    data = client_socket.recv(1024)
    
    if not data:
        break
    
    received_message = data.decode('utf-8')
    
    # 종료 요청 확인
    if received_message.lower().strip() in ['quit', 'exit']:
        response = "Echo 서버 연결을 종료합니다. 안녕히 가세요!"
        client_socket.send(response.encode('utf-8'))
        break
    
    # Echo 메시지 전송
    echo_message = f"Echo: {received_message}"
    client_socket.send(echo_message.encode('utf-8'))
```

**클라이언트 (echo_client.py):**
```python
while True:
    # 메시지 입력
    message = input("[입력] 메시지를 입력하세요: ")
    
    # 서버에 전송
    client_socket.send(message.encode('utf-8'))
    
    # 응답 수신
    data = client_socket.recv(1024)
    response = data.decode('utf-8')
    print(response)
    
    # 종료 확인
    if message.lower().strip() in ['quit', 'exit']:
        break
```

---

### 3. Number 서버 (포트 9003)

#### 서버 기능
- 1~100 사이의 랜덤 숫자 생성
- 클라이언트의 숫자 입력 받아 UP/DOWN 힌트 제공
- 10번의 기회 제공
- 정답 맞추면 축하 메시지 전송
- 'quit' 입력 시 정답 공개 후 종료

#### 클라이언트 기능
- 서버에 연결
- 게임 규칙 안내 수신
- 사용자로부터 숫자 입력 받아 서버에 전송
- 서버 힌트 출력
- 게임 종료 조건 처리

#### 핵심 코드 설명

**서버 (number_server.py):**
```python
# 랜덤 숫자 생성
secret_number = random.randint(1, 100)
max_attempts = 10
attempts = 0

while attempts < max_attempts:
    # 클라이언트로부터 숫자 입력 받기
    data = client_socket.recv(1024)
    user_input = data.decode('utf-8').strip()
    
    # 숫자 변환
    guess = int(user_input)
    attempts += 1
    
    # 숫자 비교 및 힌트 제공
    if guess < secret_number:
        msg = f"[시도 {attempts}/{max_attempts}] UP! 더 큰 숫자입니다."
        client_socket.send(msg.encode('utf-8'))
    elif guess > secret_number:
        msg = f"[시도 {attempts}/{max_attempts}] DOWN! 더 작은 숫자입니다."
        client_socket.send(msg.encode('utf-8'))
    else:
        msg = f"🎉 축하합니다! 정답입니다! (시도: {attempts}회)"
        client_socket.send(msg.encode('utf-8'))
        break
```

**클라이언트 (number_client.py):**
```python
# 환영 메시지 수신
welcome_data = client_socket.recv(2048)
print(welcome_data.decode('utf-8'))

# 게임 진행
while True:
    # 숫자 입력
    user_input = input("[입력] 숫자를 입력하세요 (1-100, quit=포기): ")
    
    # 서버에 전송
    client_socket.send(user_input.encode('utf-8'))
    
    # 응답 수신
    data = client_socket.recv(2048)
    response = data.decode('utf-8')
    print(response)
    
    # 게임 종료 확인
    if "축하합니다" in response or "아쉽습니다" in response:
        break
```

---

## 🧪 테스트 결과

### 테스트 환경
- **OS:** Ubuntu Linux 5.4.0
- **Python 버전:** Python 3.x
- **테스트 방법:** 로컬 루프백 (127.0.0.1)

### 1. Time 서버 테스트

**실행 결과:**
```
============================================================
[Time 서버] 서버 시작: 0.0.0.0:9001
[Time 서버] 클라이언트 연결 대기 중...
============================================================

[연결 #1] 클라이언트 접속: 127.0.0.1:xxxxx
[전송] 서버 현재 시간: 2025년 11월 03일 15시 12분 27초
[연결 종료] 클라이언트 연결 종료
```

**클라이언트 결과:**
```
============================================================
[Time 클라이언트] 서버에 연결 시도: 127.0.0.1:9001
============================================================
[연결 성공] 서버에 연결되었습니다.

[수신 메시지]
  서버 현재 시간: 2025년 11월 03일 15시 12분 27초

============================================================
[Time 클라이언트] 연결 종료
============================================================
```

**결과:** ✅ 성공 - 시간 전송 정상 작동

---

### 2. Echo 서버 테스트

**실행 결과:**
```
============================================================
[Echo 서버] 서버 시작: 0.0.0.0:9002
[Echo 서버] 클라이언트 연결 대기 중...
============================================================

[연결 #1] 클라이언트 접속: 127.0.0.1:xxxxx
[수신 #1] 안녕하세요 서버님
[전송 #1] Echo: 안녕하세요 서버님
[수신 #2] 테스트 메시지입니다
[전송 #2] Echo: 테스트 메시지입니다
[수신 #3] 종료 요청: quit
[통계] 총 3개의 메시지를 처리했습니다.
[연결 종료] 클라이언트 연결 종료
```

**클라이언트 결과:**
```
============================================================
[Echo 클라이언트] 서버에 연결 시도: 127.0.0.1:9002
============================================================
[연결 성공] 서버에 연결되었습니다.

[입력] 메시지를 입력하세요: 안녕하세요 서버님
[전송 #1] 안녕하세요 서버님
[수신 #1] Echo: 안녕하세요 서버님

[입력] 메시지를 입력하세요: 테스트 메시지입니다
[전송 #2] 테스트 메시지입니다
[수신 #2] Echo: 테스트 메시지입니다

[입력] 메시지를 입력하세요: quit
[전송 #3] quit
[수신 #3] Echo 서버 연결을 종료합니다. 안녕히 가세요!

============================================================
[Echo 클라이언트] 총 3개의 메시지를 전송했습니다.
============================================================
```

**결과:** ✅ 성공 - 양방향 메시지 교환 정상 작동, 한글 지원 확인

---

### 3. Number 서버 테스트

**실행 결과:**
```
============================================================
[Number 서버] 서버 시작: 0.0.0.0:9003
[Number 서버] 클라이언트 연결 대기 중...
============================================================

[게임 #1] 클라이언트 접속: 127.0.0.1:xxxxx
[게임 시작] 정답: 27 (클라이언트에게는 비밀)
[시도 1] 입력: 50
[응답] DOWN
[시도 2] 입력: 25
[응답] UP
[시도 3] 입력: 75
[응답] DOWN
[알림] 클라이언트가 게임을 포기했습니다.
[연결 종료] 클라이언트 연결 종료
```

**클라이언트 결과:**
```
============================================================
[Number 클라이언트] 서버에 연결 시도: 127.0.0.1:9003
============================================================
[연결 성공] 서버에 연결되었습니다.

========================================
   숫자 맞추기 게임에 오신 것을 환영합니다!
========================================
규칙:
  - 1부터 100 사이의 숫자를 맞춰보세요.
  - 기회는 10번 있습니다.
  - 'quit'를 입력하면 포기합니다.
========================================

[입력] 숫자를 입력하세요 (1-100, quit=포기): 50
[시도 1/10] DOWN! 더 작은 숫자입니다. (남은 기회: 9)

[입력] 숫자를 입력하세요 (1-100, quit=포기): 25
[시도 2/10] UP! 더 큰 숫자입니다. (남은 기회: 8)

[입력] 숫자를 입력하세요 (1-100, quit=포기): 75
[시도 3/10] DOWN! 더 작은 숫자입니다. (남은 기회: 7)

[입력] 숫자를 입력하세요 (1-100, quit=포기): quit

게임을 포기하셨습니다. 정답은 27이었습니다.

============================================================
[Number 클라이언트] 게임 종료
============================================================
```

**결과:** ✅ 성공 - 게임 로직 정상 작동, 상태 유지 통신 확인

---

## 🔧 기술적 특징

### 1. TCP/IP 소켓 프로그래밍
- **소켓 생성:** `socket.socket(AF_INET, SOCK_STREAM)`
- **주소 바인딩:** `bind((host, port))`
- **연결 대기:** `listen(backlog)`
- **연결 수락:** `accept()`
- **데이터 송수신:** `send()`, `recv()`

### 2. 문자 인코딩
- UTF-8 인코딩으로 한글 지원
- `encode('utf-8')` / `decode('utf-8')`

### 3. 예외 처리
- ConnectionRefusedError 처리
- KeyboardInterrupt 처리 (Ctrl+C)
- 일반 예외 처리

### 4. 소켓 옵션
- `SO_REUSEADDR` 옵션으로 주소 재사용 허용
- 서버 재시작 시 "Address already in use" 오류 방지

### 5. 통신 패턴
- **단순 요청-응답:** Time 서버
- **연속 메시지 교환:** Echo 서버
- **상태 유지 통신:** Number 서버

---

## 🌐 크로스 플랫폼 테스트 방법

### Windows 서버 + Linux 클라이언트

**Windows (서버):**
```cmd
> ipconfig
IPv4 주소: 192.168.1.200

> cd tcp_socket_programming\1_time_server
> python time_server.py
```

**Linux (클라이언트):**
```bash
$ python3 1_time_server/time_client.py 192.168.1.200
```

### Linux 서버 + Windows 클라이언트

**Linux (서버):**
```bash
$ hostname -I
192.168.1.100

$ python3 1_time_server/time_server.py
```

**Windows (클라이언트):**
```cmd
> python 1_time_server\time_client.py 192.168.1.100
```

---

## 📊 학습 내용 및 성과

### 1. 네트워크 프로그래밍 개념 이해
- TCP/IP 프로토콜의 동작 원리
- 클라이언트-서버 아키텍처
- 소켓 프로그래밍 API

### 2. 다양한 통신 패턴 구현
- 단방향 통신 (Time 서버)
- 양방향 통신 (Echo 서버)
- 상태 유지 통신 (Number 서버)

### 3. 실제 네트워크 애플리케이션 개발
- 서버-클라이언트 프로그램 설계
- 프로토콜 정의 및 구현
- 예외 처리 및 오류 관리

### 4. 크로스 플랫폼 호환성
- Windows와 Linux 간 통신
- 플랫폼 독립적인 코드 작성

---

## 💡 프로그램 활용 방안

### 1. Time 서버
- 네트워크 시간 동기화
- 분산 시스템의 타임스탬프 제공
- 로그 시스템의 시간 기준

### 2. Echo 서버
- 네트워크 연결 테스트
- 패킷 전송 확인
- 네트워크 지연 시간 측정

### 3. Number 서버
- 온라인 게임 서버 프로토타입
- 대화형 애플리케이션 학습
- 세션 관리 연습

---

## 🎯 결론

본 과제를 통해 TCP/IP 소켓 프로그래밍의 기본 개념부터 실제 구현까지
전 과정을 경험할 수 있었습니다. 

3가지 서버-클라이언트 프로그램을 구현하면서:
- 소켓 생성 및 관리
- 데이터 송수신
- 예외 처리
- 다양한 통신 패턴

등을 학습하였으며, 모든 프로그램이 정상적으로 작동함을 확인했습니다.

특히 Windows와 Linux 간 크로스 플랫폼 통신이 가능하도록 설계하여
실제 네트워크 환경에서 사용할 수 있는 실용적인 프로그램을 완성했습니다.

---

## 📚 참고 자료

1. Python Socket Programming Documentation
   - https://docs.python.org/3/library/socket.html

2. TCP/IP Protocol Suite
   - Forouzan, Behrouz A. "TCP/IP Protocol Suite"

3. Computer Networking: A Top-Down Approach
   - Kurose, James F., Ross, Keith W.

4. RFC 793 - Transmission Control Protocol
   - https://www.rfc-editor.org/rfc/rfc793

---

## 📂 제출 파일 목록

1. **소스 코드 (6개 파일)**
   - `1_time_server/time_server.py`
   - `1_time_server/time_client.py`
   - `2_echo_server/echo_server.py`
   - `2_echo_server/echo_client.py`
   - `3_number_server/number_server.py`
   - `3_number_server/number_client.py`

2. **문서 파일**
   - `README.md` - 상세 사용 설명서
   - `실행방법.txt` - 빠른 실행 가이드
   - `테스트결과.txt` - 테스트 결과 상세 보고
   - `보고서_제출용.md` - 본 보고서

3. **기타**
   - `테스트_스크립트.sh` - 자동 테스트 스크립트

---

**제출일: 2025년 11월 3일**

**과제 완료**

